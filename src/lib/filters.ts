/**
 * Sentinel meaning "this facet is not narrowing the list".
 *
 * Shared by every filterable list so an inactive facet has one representation
 * across filter state, the filter menus, and the chip row.
 */
export const ALL_FILTER_VALUE = "all";
